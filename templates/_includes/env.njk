<script>
const LAYOUT = "{{ layout }}"
const PERMALINK = "{{ permalink }}"
const DEVELOPMENT = {{ development }}
const SCRIPT_ID = "{{ script_id }}"
const API_URL = "{{ api_url }}"
const EMBEDDED = {{ embedded }}
const LANGUAGE_CODE = "{{ language_code }}"
const TITLE = "{{ title }}"
const COMPANY = "{{ company }}"
const WEBSITE_URL = "{{ website_url }}"
const MEMBERS_SPREADSHEET_ID = "{{ members_spreadsheet_id }}"
const MEMBERS_SHEET_NAME = "{{ members_sheet_name }}"
const EVENTS_SPREADSHEET_ID = "{{ events_spreadsheet_id }}"
const EVENTS_SHEET_NAME = "{{ events_sheet_name }}"
const TRAINING_SPREADSHEET_ID = "{{ training_spreadsheet_id }}"
const ASSISTANCE_SHEET_NAME = "{{ assistance_sheet_name }}"
const MEMBER_CACHE = "{{ member_cache }}"
const EVENT_CACHE = "{{ event_cache }}"
const TRAINING_CACHE = "{{ training_cache }}"
const USER_CREDS = "{{ user_creds }}"
const USER_SESSION = "{{ user_session }}"
const SESSION_TOKEN = "{{ session_token }}"
</script>
