<!DOCTYPE html>
<html lang="{{ language_code }}">
    <head>
        <base target="_top">
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
        <meta http-equiv="Pragma" content="no-cache">
        <meta http-equiv="Expires" content="0">
        <meta name="theme-color" content="#7C3AED">
        <meta name="theme-color" content="#7C3AED" media="(prefers-color-scheme: light)">
        <meta name="theme-color" content="#7C3AED" media="(prefers-color-scheme: dark)">
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
        <meta name="apple-mobile-web-app-capable" content="yes">
        <meta name="mobile-web-app-capable" content="yes">
        <meta name="apple-mobile-web-app-title" content="{{ title }}">
        <meta name="application-name" content="{{ title }}">
        <meta name="msapplication-TileColor" content="#7C3AED">
        
        <title>{{ title }}</title>

        {% if embedded %}
        {% else %}
        <link rel="icon" type="image/x-icon" href="favicon.ico">
        {% endif %}

        {% if embedded %}
        {% else %}
        <link rel="manifest" href="manifest.webmanifest">
        {% endif %}
        
        {% if embedded %}
        <style>
        {% include "../../docs/styles/main.css" %}
        </style>
        {% else %}
        <link rel="stylesheet" href="styles/main.css">
        {% endif %}
    </head>
    <body>
        {% include "loader.njk" %}
        
        {% include "app.njk" %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/logger.js" %}
        </script>
        {% else %}
        <script src="scripts/logger.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/dances.js" %}
        </script>
        {% else %}
        <script src="scripts/dances.js" defer></script>
        {% endif %}

        {% include "env.njk" %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/cache.js" %}
        </script>
        {% else %}
        <script src="scripts/cache.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/api_client.js" %}
        </script>
        {% else %}
        <script src="scripts/api_client.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/app.js" %}
        </script>
        {% else %}
        <script src="scripts/app.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/navigation.js" %}
        </script>
        {% else %}
        <script src="scripts/navigation.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/utils.js" %}
        </script>
        {% else %}
        <script src="scripts/utils.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/auth.js" %}
        </script>
        {% else %}
        <script src="scripts/auth.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/members.js" %}
        </script>
        {% else %}
        <script src="scripts/members.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/events.js" %}
        </script>
        {% else %}
        <script src="scripts/events.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/trainings.js" %}
        </script>
        {% else %}
        <script src="scripts/trainings.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/dashboard.js" %}
        </script>
        {% else %}
        <script src="scripts/dashboard.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/validation.js" %}
        </script>
        {% else %}
        <script src="scripts/validation.js" defer></script>
        {% endif %}

        {% if embedded %}
        <script>
        {% include "../../docs/scripts/button-feedback.js" %}
        </script>
        {% else %}
        <script src="scripts/button-feedback.js" defer></script>
        {% endif %}
    </body>
</html>